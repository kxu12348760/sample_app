<li>
	<%= gravatar_for user, size: 52 %>
	<%= link_to user.name, user %>
	<% if current_user.admin? %>
	  <% if user.admin? %>
	    (admin)
	  <% end %>
	  <% if !current_user?(user) %>
	    | <%= link_to "delete", user, method: :delete, data: { confirm: "You sure?" } %>
	    <% if !user.admin? %>
	  	  | <%= link_to "promote to admin", promote_user_path(user) %>
	    <% end %>
	  <% end %>
	<% end %> 
</li>
